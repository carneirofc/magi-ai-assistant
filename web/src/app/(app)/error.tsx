"use client";

// Error boundary for the dashboard group. The body lives in the library
// (DashboardError); this client file mounts it as the group's error boundary.

export { default } from "@carneirofc/magi-web/components/DashboardError";
