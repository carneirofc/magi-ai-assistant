// A user's memory. The view lives in the library; this file mounts it at
// /memory/[user] (Next passes the route params through to the view).

export { default } from "@carneirofc/magi-web/pages/memory-user";

export const dynamic = "force-dynamic";
