// Dashboard overview. The view lives in the library; this file mounts it at /.
// Pass `copy` to DashboardPage to reskin the header (see @carneirofc/magi-web/pages/dashboard).

export { default } from "@carneirofc/magi-web/pages/dashboard";

export const dynamic = "force-dynamic";
